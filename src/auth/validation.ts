// STUB (tester) — signature only; dev implements field-level rules. (spec §5)
import type { RegistrationInput, FieldErrors } from './types'

export function validateRegistration(input: RegistrationInput): FieldErrors {
  throw new Error('not implemented')
}
